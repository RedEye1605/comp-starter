# {{ project_name }} Configuration
# Generated: {{ date }}

# Paths
data:
  raw_dir: data/raw
  processed_dir: data/processed
  external_dir: data/external

# Model defaults
model:
  type: random_forest
  random_state: 42
  n_estimators: 100
  max_depth: null

# Cross-validation
cv:
  n_folds: 5
  shuffle: true
  random_state: 42

# Training
training:
  test_size: 0.2
  random_state: 42

# Submissions
submission:
  id_col: id
  target_col: target
